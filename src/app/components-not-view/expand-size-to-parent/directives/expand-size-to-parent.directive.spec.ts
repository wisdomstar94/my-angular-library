import { ExpandSizeToParentDirective } from './expand-size-to-parent.directive';

describe('ExpandSizeToParentDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpandSizeToParentDirective();
    expect(directive).toBeTruthy();
  });
});
