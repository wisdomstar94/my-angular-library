import { ElementSizePositionInfoDirective } from './element-size-position-info.directive';

describe('ElementSizePositionInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementSizePositionInfoDirective();
    expect(directive).toBeTruthy();
  });
});
