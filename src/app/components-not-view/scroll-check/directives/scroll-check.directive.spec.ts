import { ScrollCheckDirective } from './scroll-check.directive';

describe('ScrollCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollCheckDirective();
    expect(directive).toBeTruthy();
  });
});
