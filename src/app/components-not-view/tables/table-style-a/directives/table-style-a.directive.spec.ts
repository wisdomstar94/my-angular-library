import { TableStyleADirective } from './table-style-a.directive';

describe('TableStyleADirective', () => {
  it('should create an instance', () => {
    const directive = new TableStyleADirective();
    expect(directive).toBeTruthy();
  });
});
