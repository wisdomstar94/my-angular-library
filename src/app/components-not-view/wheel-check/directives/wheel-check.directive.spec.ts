import { WheelCheckDirective } from './wheel-check.directive';

describe('WheelCheckDirective', () => {
  it('should create an instance', () => {
    const directive = new WheelCheckDirective();
    expect(directive).toBeTruthy();
  });
});
