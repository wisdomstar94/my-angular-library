import { CkeditorTextareaCustomDirective } from './ckeditor-textarea-custom.directive';

describe('CkeditorTextareaCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new CkeditorTextareaCustomDirective();
    expect(directive).toBeTruthy();
  });
});
