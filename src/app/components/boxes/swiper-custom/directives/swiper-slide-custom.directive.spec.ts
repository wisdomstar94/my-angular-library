import { SwiperSlideCustomDirective } from './swiper-slide-custom.directive';

describe('SwiperSlideCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperSlideCustomDirective();
    expect(directive).toBeTruthy();
  });
});
