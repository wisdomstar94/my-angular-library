import { FullPageItemDirective } from './full-page-item.directive';

describe('FullPageItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FullPageItemDirective();
    expect(directive).toBeTruthy();
  });
});
